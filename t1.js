/*jshint esversion: 6 */
function reverseVector(vector) {
    return [!vector[0], !vector[1]];
}

let direction = {
    straight: [1, 1],
    left: [-1, 1],
    right: [1, -1]
};

let wallsAtDirection = direction.

console.log(direction);